# Website Downloader - 环境变量配置模板
# 复制此文件为 .env.local 并根据需要修改

# ========== 服务器配置 ==========
# 应用监听端口 (默认: 6868)
PORT=6868

# 应用运行环境 (production | development, 默认: production)
NODE_ENV=production

# ========== 下载配置 ==========
# 下载超时时间，单位：分钟 (默认: 20)
DOWNLOAD_TIMEOUT=20

# 最大下载文件大小，单位：MB (默认: 0，表示无限制)
# MAX_DOWNLOAD_SIZE=5000

# ========== 文件路径配置 ==========
# 临时文件夹路径 (默认: ./tmp)
# 可用于指定大容量磁盘位置，特别是在内网部署时
# TMP_DIR=./tmp

# 输出文件夹路径 (默认: ./public/sites)
# 下载的 ZIP 文件保存位置
# OUTPUT_DIR=./public/sites

# ========== Windows 特定配置 ==========
# Windows 下 wget.exe 的完整路径
# 如果不设置，会自动搜索：
#  1. 项目 bin/wget.exe
#  2. 系统 PATH 中的 wget
# Windows 示例:
# WGET_PATH=C:\tools\wget.exe
# 或使用 Scoop 安装的 wget:
# WGET_PATH=C:\Users\YourUsername\scoop\shims\wget.exe

# ========== 日志配置 ==========
# 日志级别 (error | warn | info | debug, 默认: info)
LOG_LEVEL=info

# ========== 可选：自动清理 ==========
# 是否自动清理临时文件 (true | false, 默认: false)
# AUTO_CLEAN_TMP=false

# ========== 内网代理配置 ==========
# 如果在内网环境无法直接访问外部网络，配置代理
# HTTP_PROXY=http://proxy.internal:8080
# HTTPS_PROXY=http://proxy.internal:8080
# NO_PROXY=*.internal.local,127.0.0.1,localhost
